#!/usr/bin/env bash

# pre-commit hook
# Checks if the code is properly formatted before committing.

echo "🔍 正在检查代码格式 (cargo fmt)..."

if ! cargo fmt --all -- --check; then
    echo "❌ 错误: 代码格式不规范。"
    echo "👉 请运行 'cargo fmt --all' 修复格式问题后重新提交。"
    exit 1
fi

echo "✅ 代码格式检查通过！"
exit 0
